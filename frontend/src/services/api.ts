import { session } from '../features/auth/session';
export type ApiError={code:string;message:string}; const base=process.env.NEXT_PUBLIC_API_BASE_URL??'http://localhost:4000/api/v1'; export async function api<T>(path:string,init:RequestInit={}):Promise<T>{const token=session.token();const isFormData=typeof FormData!=='undefined'&&init.body instanceof FormData;const res=await fetch(`${base}${path}`,{...init,headers:{...(isFormData?{}:{'Content-Type':'application/json'}),...(token?{Authorization:`Bearer ${token}`}:{}),...init.headers}});if(!res.ok){const body=await res.json().catch(()=>null);throw body?.error as ApiError ?? {code:'NETWORK_ERROR',message:'Request failed.'}}return res.status===204?undefined as T:res.json() as Promise<T>}
