import { api } from './api'; import { Task, TaskInput, TaskList } from '../types/task';
export type TaskFilters={page?:number;limit?:number;status?:string;priority?:string;sortBy?:string;sortOrder?:string;dueFrom?:string;dueTo?:string};
export const tasksApi={ list:(filters:TaskFilters={})=>{const query=new URLSearchParams();Object.entries(filters).forEach(([key,value])=>{if(value!==undefined&&value!=='')query.set(key,String(value))});return api<TaskList>(`/tasks?${query}`)}, get:(id:string)=>api<{data:Task}>(`/tasks/${id}`), create:(body:TaskInput)=>api<{data:Task}>('/tasks',{method:'POST',body:JSON.stringify(body)}), update:(id:string,body:TaskInput)=>api<{data:Task}>(`/tasks/${id}`,{method:'PATCH',body:JSON.stringify(body)}), remove:(id:string)=>api<void>(`/tasks/${id}`,{method:'DELETE'}) };
