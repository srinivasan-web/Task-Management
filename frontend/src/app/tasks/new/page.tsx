'use client';
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import { RequireAuth } from '../../../features/auth/require-auth'; import { tasksApi } from '../../../services/tasks-api';
export default function NewTaskPage(){const router=useRouter(),[error,setError]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);try{await tasksApi.create({title:String(form.get('title')),description:String(form.get('description'))||undefined,priority:form.get('priority') as 'LOW'|'MEDIUM'|'HIGH',dueDate:form.get('dueDate')?new Date(String(form.get('dueDate'))).toISOString():undefined,location:String(form.get('location'))||undefined});router.replace('/dashboard')}catch(e){setError((e as {message?:string}).message??'Could not create task.')}}return <RequireAuth><main className="auth-page"><section className="auth-card"><h1>New task</h1><form className="auth-form" onSubmit={submit}><label>Title<input name="title" required maxLength={200}/></label><label>Description<textarea name="description"/></label><label>Priority<select name="priority" defaultValue="MEDIUM"><option value="LOW">Low</option><option value="MEDIUM">Medium</option><option value="HIGH">High</option></select></label><label>Due date<input name="dueDate" type="datetime-local"/></label><label>Location<input name="location"/></label>{error&&<p className="form-error">{error}</p>}<button>Create task</button></form></section></main></RequireAuth>}
