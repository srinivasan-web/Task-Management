import { ServiceUnavailableException } from '@nestjs/common'; import { WeatherService } from './weather.service';
describe('WeatherService', () => { const config = { get: jest.fn().mockReturnValue('weather-key') }; const service = new WeatherService(config as any); beforeEach(() => { jest.clearAllMocks(); }); it('normalizes provider data', async () => { global.fetch = jest.fn().mockResolvedValue({ ok: true, json: async () => ({ name: 'Chennai', main: { temp: 31, humidity: 72 }, weather: [{ main: 'Clouds' }], wind: { speed: 4.2 } }) }); await expect(service.current('Chennai')).resolves.toEqual({ location: 'Chennai', temperature: 31, condition: 'Clouds', humidity: 72, windSpeed: 4.2 }); }); it('does not expose provider failures', async () => { global.fetch = jest.fn().mockResolvedValue({ ok: false }); await expect(service.current('Chennai')).rejects.toBeInstanceOf(ServiceUnavailableException); }); });
