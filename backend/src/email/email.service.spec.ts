import { EmailService } from './email.service';
describe('EmailService', () => { it('does not call a provider when email is unconfigured', async () => { const config = { get: jest.fn().mockReturnValue(undefined) }; const service = new EmailService(config as any); global.fetch = jest.fn(); await service.sendTaskCreated('a@example.com', { id: 'task-a', title: 'Task', priority: 'HIGH', dueDate: null, location: null }); expect(global.fetch).not.toHaveBeenCalled(); }); it('swallows provider failure after persistence', async () => { const config = { get: jest.fn((key: string, fallback?: string) => key === 'RESEND_API_KEY' ? 'key' : fallback) }; const service = new EmailService(config as any); global.fetch = jest.fn().mockResolvedValue({ ok: false, status: 500 }); await expect(service.sendTaskCompleted('a@example.com', { id: 'task-a', title: 'Task', priority: 'HIGH', dueDate: null, location: null })).resolves.toBeUndefined(); }); });
